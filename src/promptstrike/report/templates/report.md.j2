# PromptStrike Penetration-Test Report

Black-box LLM red-team assessment · OWASP LLM Top 10 (2025)

| | |
|---|---|
| **Target** | {{ target }} |
| **Scan date** | {{ scanned_at }} |
| **Scope** | {{ tested_count }} of 10 categories tested · {{ out_of_scope_count }} out of scope |
| **Confirmed findings** | {{ total_findings }} |

## Executive summary

**Severity distribution:** {% for name, count, color in severity_counts %}{{ name }} {{ count }}{% if not loop.last %} · {% endif %}{% endfor %}

{{ narrative }}

## Coverage matrix

What was in scope for this black-box scan and what was not.

| OWASP LLM | Category | Status | Findings | Notes |
|---|---|---|---|---|
{% for row in coverage %}| `{{ row.owasp_id }}` | {{ row.title }} | {{ row.status }} | {{ row.findings }} | {{ row.note }} |
{% endfor %}

## Findings

{% if not findings %}
No confirmed findings in the tested categories.
{% endif %}
{% for f in findings %}
### {{ f.index }}. {{ f.title }} — `{{ f.severity }}`

- **OWASP LLM:** {{ f.owasp_id }} — {{ f.owasp_title }}
- **MITRE ATLAS:** {{ f.atlas_technique }}{% if f.atlas_name != f.atlas_technique %} ({{ f.atlas_name }}){% endif %}

- **Judge confidence:** {{ f.confidence }}{% if f.duplicate_count > 1 %} · {{ f.duplicate_count }} near-identical occurrences{% endif %}

- **Severity vector:** `{{ f.vector }}`
- _{{ f.justification }}_

**Impact.** {{ f.impact }}

**Reproduction.**
{% for step in f.reproduction %}{{ loop.index }}. {{ step }}
{% endfor %}

**Evidence — request (attack payload):**

```text
{{ f.request }}
```

**Evidence — target response (redacted, excerpt):**

```text
{{ f.response_excerpt }}
```
{% if f.evidence %}
**Judge-cited snippet (redacted):**

```text
{{ f.evidence }}
```
{% endif %}

**Remediation.**
{% for step in f.remediation %}- {{ step }}
{% endfor %}

---
{% endfor %}

## GRC appendix — control cross-reference

Findings mapped to governance and regulatory frameworks.

| Finding | NIST AI RMF | ISO/IEC 42001 | EU AI Act |
|---|---|---|---|
{% for row in grc_rows %}| `{{ row.owasp_id }}` {{ row.title }} | {{ row.nist | join("; ") }} | {{ row.iso | join("; ") }} | {{ row.eu | join("; ") }} |
{% endfor %}
{% if not grc_rows %}_No findings to cross-reference._
{% endif %}

> {{ grc_caveat }}

---

**Authorised use only.** This report documents a security test of a system the
operator owns or has explicit written permission to test. Evidence is redacted on
a best-effort basis; treat this document as sensitive.

_Generated by {{ tool }} v{{ version }} · {{ generated_at }}_
